import * as AnalyticsService from '../analyticsService';

describe('AnalyticsService', () => {
  it('should have exported functions', () => {
    expect(typeof AnalyticsService).toBe('object');
    // Optionally log the keys for debugging
    // console.log(Object.keys(AnalyticsService));
  });
  // Add more specific tests for each exported function as needed
}); 